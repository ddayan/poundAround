<h1> Create new level </h1>
 
<%= form_for(@level) do |f| %>
  <% if @level.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@level.errors.count, "error") %> prohibited this post from being saved:</h2>
    <ul>
    <% @level.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  

  
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Currency Value" %><br />
    <%= f.text_field :currency_val %>
  </div>
  <div class="field">
    <%= f.label "Currency Symbol" %><br />
    <%= f.text_field :currency_sym %>
  </div>
 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
 
<%= link_to 'Back', levels_path %>